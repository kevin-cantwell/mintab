{
    "manifest_version": 3,
    "name": "Avoid Tab Duplicates",
    "version": "0.0.0.1",
    "description": "Watches for newly created tabs and if the URL matches an open tab you will instead be redirected to it instead.",
    "permissions": ["tabs", "windows"],
    "background": {
        "service_worker": "background.js"
    },
    "action": {
        "default_title": "Avoid Tab Duplicates"
    }
}
